Red-teaming lab on this Windows: Ollama (localhost:11434), ~/.venv-redteam, dashboard at ~/redteam-lab/start-dashboard.bat -> :8080. User wants ONE folder, bat launchers, no manual cmds. Models are text-only, tinydolphin faster but refuses. RTX 3050 4GB WSL2. Video gen interest: fantasy anime LoRA "f4nt4sy4n1m6". Prefers curl/aria2c over HF hub. Prefers uncensored models.

On this Windows machine, the user has previously seen Ollama appear "off" before starting, so they expect me to verify localhost:11434 is actually responsive immediately rather than assuming it is up.

ViralHooks app: User removed email/password auth (2nd option). Only Google OAuth remains as login method. User used OpenCode CLI to make this change so code may differ from what's in /tmp/viralhooks. Keep this in mind for all future auth flow checks.

ViralHooks backup skill: backup/viralhooks-backup. Repo: /tmp/viralhooks (master). Deploy: /tmp/gh-pages-deploy (push HEAD:gh-pages). Auto-backup when asked.

ViralHooks: User removed email/password (2nd option) via OpenCode — only Google OAuth remains.
